apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: checkpoint-volume
  namespace: merlin
spec:
  storageClassName: shared
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 150Gi