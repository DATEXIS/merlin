# Copy to `.env` (or export in your shell) before running anything.
# Never commit the filled-in version.

# Weights & Biases — experiment tracking, generation artifacts, eval results.
WANDB_API_KEY=
WANDB_ENTITY=anon-entity
WANDB_PROJECT=MERLIN-DDX-SFT

# Hugging Face — base-model downloads (Qwen3, Llama-3.x, encoder baselines).
HUGGING_FACE_HUB_TOKEN=

# vLLM OpenAI-compatible endpoint. Ignored when serving in-cluster.
OPENAI_API_KEY=openai-abc-key
